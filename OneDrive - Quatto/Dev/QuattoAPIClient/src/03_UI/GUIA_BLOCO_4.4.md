# ?? BLOCO 4.4 - GroupBox Organization (Template & Guide)

**Status**: ? PRONTO PARA IMPLEMENTAÇÃO  
**Tempo Estimado**: 0.5-1 hora  
**Prioridade**: ?? RÁPIDA MAS IMPACTANTE  
**Marco**: Organização visual com GroupBox  

---

## ESTRATÉGIA

Adicionar GroupBox em tabs específicas para agrupar controles relacionados.

### Tabs com GroupBox
```
? General Tab:           1 GroupBox (API Connection)
? Incremental Tab:       1 GroupBox (Incremental Config)
? Storage Tab:           2 GroupBoxes (Storage + Processing)
? Pagination Tab:        Sem agrupamento (3 controls simples)
? Advanced Tab:          Sem agrupamento (5 controls relacionados)
```

---

## TEMPLATE: General Tab com GroupBox

```csharp
private void CreateGeneralTab()
{
    var tab = new TabPage("Geral");
    tab.Padding = new Padding(10);
    tabControl!.TabPages.Add(tab);

    int y = 10;
    const int labelWidth = 150;
    const int controlWidth = 300;
    const int controlHeight = 24;
    const int rowHeight = 35;
    const int leftMargin = 10;
    const int labelControlGap = 10;
    const int groupBoxHeight = 300;  // Altura do grupo

    // ???????????????????????????????????????????????????????????????
    // GROUP BOX: API CONNECTION
    // ???????????????????????????????????????????????????????????????
    var grpApiConnection = new GroupBox
    {
        Text = "Configuração da API",
        Location = new Point(leftMargin, y),
        Width = tab.Width - (2 * leftMargin) - 20,  // Preenche a aba
        Height = groupBoxHeight,
        ForeColor = Color.DarkBlue,
        Font = new Font("Segoe UI", 10f, FontStyle.Bold)
    };
    tab.Controls.Add(grpApiConnection);

    // Coordenadas dentro do GroupBox
    int groupY = 20;

    // ???????????????????????????????????????????????????????????????
    // CONEXÃO (dentro do GroupBox)
    // ???????????????????????????????????????????????????????????????
    var lblConnection = new Label
    {
        Text = "Conexão:",
        Location = new Point(leftMargin, groupY),
        Width = labelWidth,
        Height = controlHeight
    };
    grpApiConnection.Controls.Add(lblConnection);

    cmbConnection = new ComboBox
    {
        Location = new Point(leftMargin + labelWidth + labelControlGap, groupY),
        Width = controlWidth,
        Height = controlHeight,
        DropDownStyle = System.Windows.Forms.ComboBoxStyle.DropDownList
    };
    grpApiConnection.Controls.Add(cmbConnection);
    groupY += rowHeight;

    // ???????????????????????????????????????????????????????????????
    // BASE URL (dentro do GroupBox)
    // ???????????????????????????????????????????????????????????????
    var lblBaseUrl = new Label
    {
        Text = "Base URL:",
        Location = new Point(leftMargin, groupY),
        Width = labelWidth,
        Height = controlHeight
    };
    grpApiConnection.Controls.Add(lblBaseUrl);

    txtBaseUrl = new TextBox
    {
        Location = new Point(leftMargin + labelWidth + labelControlGap, groupY),
        Width = controlWidth,
        Height = controlHeight,
        Text = "https://",
        Multiline = false
    };
    txtBaseUrl.Leave += (s, e) => ValidateBaseUrl();
    grpApiConnection.Controls.Add(txtBaseUrl);
    groupY += rowHeight;

    // ???????????????????????????????????????????????????????????????
    // ENDPOINT (dentro do GroupBox)
    // ???????????????????????????????????????????????????????????????
    var lblEndpoint = new Label
    {
        Text = "Endpoint:",
        Location = new Point(leftMargin, groupY),
        Width = labelWidth,
        Height = controlHeight
    };
    grpApiConnection.Controls.Add(lblEndpoint);

    txtEndpoint = new TextBox
    {
        Location = new Point(leftMargin + labelWidth + labelControlGap, groupY),
        Width = controlWidth,
        Height = controlHeight,
        Text = "/v1/",
        Multiline = false
    };
    grpApiConnection.Controls.Add(txtEndpoint);
    groupY += rowHeight;

    // ???????????????????????????????????????????????????????????????
    // PAGE SIZE (dentro do GroupBox)
    // ???????????????????????????????????????????????????????????????
    var lblPageSize = new Label
    {
        Text = "Tamanho Página:",
        Location = new Point(leftMargin, groupY),
        Width = labelWidth,
        Height = controlHeight
    };
    grpApiConnection.Controls.Add(lblPageSize);

    numPageSize = new NumericUpDown
    {
        Location = new Point(leftMargin + labelWidth + labelControlGap, groupY),
        Width = 150,
        Height = controlHeight,
        Value = 500,
        Minimum = 1,
        Maximum = 10000,
        Increment = 100,
        DecimalPlaces = 0
    };
    numPageSize.Leave += (s, e) => ValidatePageSize();
    grpApiConnection.Controls.Add(numPageSize);
}
```

---

## TEMPLATE: Incremental Tab com GroupBox

```csharp
private void CreateIncrementalTab()
{
    var tab = new TabPage("Incremental");
    tab.Padding = new Padding(10);
    tabControl!.TabPages.Add(tab);

    int y = 10;
    const int labelWidth = 150;
    const int controlWidth = 300;
    const int controlHeight = 24;
    const int rowHeight = 35;
    const int leftMargin = 10;
    const int labelControlGap = 10;

    // ???????????????????????????????????????????????????????????????
    // GROUP BOX: INCREMENTAL CONFIG
    // ???????????????????????????????????????????????????????????????
    var grpIncrementalConfig = new GroupBox
    {
        Text = "Configuração Incremental",
        Location = new Point(leftMargin, y),
        Width = tab.Width - (2 * leftMargin) - 20,
        Height = 150,
        ForeColor = Color.DarkGreen,
        Font = new Font("Segoe UI", 10f, FontStyle.Bold)
    };
    tab.Controls.Add(grpIncrementalConfig);

    int groupY = 20;

    // Controles dentro do GroupBox
    // (copiar código dos controles do template anterior)
    
    y = grpIncrementalConfig.Bottom + 20;

    // ???????????????????????????????????????????????????????????????
    // ENVIRONMENT (fora do GroupBox)
    // ???????????????????????????????????????????????????????????????
    var lblEnvironment = new Label
    {
        Text = "Ambiente:",
        Location = new Point(leftMargin, y),
        Width = labelWidth,
        Height = controlHeight
    };
    tab.Controls.Add(lblEnvironment);

    cmbEnvironment = new ComboBox
    {
        Location = new Point(leftMargin + labelWidth + labelControlGap, y),
        Width = 150,
        Height = controlHeight,
        DropDownStyle = System.Windows.Forms.ComboBoxStyle.DropDownList
    };
    cmbEnvironment.Items.AddRange(new object[] { "DEV", "HML", "PRD" });
    cmbEnvironment.SelectedIndex = 2;
    tab.Controls.Add(cmbEnvironment);
}
```

---

## TEMPLATE: Storage Tab com 2 GroupBoxes

```csharp
private void CreateStorageTab()
{
    var tab = new TabPage("Armazenamento");
    tab.Padding = new Padding(10);
    tabControl!.TabPages.Add(tab);

    int y = 10;
    const int labelWidth = 150;
    const int controlWidth = 300;
    const int controlHeight = 24;
    const int rowHeight = 35;
    const int leftMargin = 10;
    const int labelControlGap = 10;

    // ???????????????????????????????????????????????????????????????
    // GROUP BOX 1: STORAGE MODE
    // ???????????????????????????????????????????????????????????????
    var grpStorageMode = new GroupBox
    {
        Text = "Modo de Armazenamento",
        Location = new Point(leftMargin, y),
        Width = tab.Width - (2 * leftMargin) - 20,
        Height = 120,
        ForeColor = Color.DarkOrange,
        Font = new Font("Segoe UI", 10f, FontStyle.Bold)
    };
    tab.Controls.Add(grpStorageMode);

    int groupY = 20;

    // Controles (Modo, Alvo)
    // ...

    y = grpStorageMode.Bottom + 20;

    // ???????????????????????????????????????????????????????????????
    // GROUP BOX 2: PROCESSING OPTIONS
    // ???????????????????????????????????????????????????????????????
    var grpProcessing = new GroupBox
    {
        Text = "Opções de Processamento",
        Location = new Point(leftMargin, y),
        Width = tab.Width - (2 * leftMargin) - 20,
        Height = 100,
        ForeColor = Color.DarkRed,
        Font = new Font("Segoe UI", 10f, FontStyle.Bold)
    };
    tab.Controls.Add(grpProcessing);

    groupY = 20;

    // CheckBoxes (Compress, Hash)
    // ...
}
```

---

## ESTILO DOS GROUPBOX

```csharp
Padrão:
?? Font: Segoe UI, 10pt, Bold (título)
?? ForeColor: Cores diferentes por seção
?  ?? DarkBlue para API
?  ?? DarkGreen para Incremental
?  ?? DarkOrange para Storage
?  ?? DarkRed para Processing
?? Height: Ajustado ao conteúdo
?? Width: Preenche a aba (-20px padding)
?? Text: Descritivo e em português
```

---

## CORES SUGERIDAS

```csharp
GroupBox Colors:
?? API Connection:        Color.DarkBlue (#00008B)
?? Incremental:          Color.DarkGreen (#006400)
?? Storage:              Color.DarkOrange (#FF8C00)
?? Processing:           Color.DarkRed (#8B0000)
?? Pagination:           Color.DarkCyan (#008B8B)
?? Advanced:             Color.DarkViolet (#9400D3)
```

---

## BENEFÍCIOS

```
? Melhor organização visual
? Menos confusão
? Agrupa relacionados
? Mais profissional
? Facilita leitura
```

---

## ? PRÓXIMOS PASSOS

1. **Implementar GroupBox na General Tab** (10 min)
2. **Implementar GroupBox na Incremental Tab** (10 min)
3. **Implementar GroupBox na Storage Tab** (10 min)
4. **Compilar e validar** (5 min)
5. **Próximo: BLOCO 5 (Testes)** (6-8h)

Total: ~0.5-1h

---

**Status**: ?? **PRONTO PARA IMPLEMENTAÇÃO**  
**Próximo**: BLOCO 4.4 (GroupBox Implementation)

